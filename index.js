'use strict';

import     firebaseFactory from "./services/firebase-service.js";

const getResume = async () => {

 /*   let teste = await firebaseFactory.create('teste',{
        teste:'asdf'
    })
    console.log(teste);
    
    debugger; */

 /*    const resume = await firebaseFactory.read('resume');
    const files = await firebaseFactory.read('files'); */
    


/*     setEducation(resume.education);
    setEmployments(resume.employments);
    setPersonalInfo(resume.personal_info);

    setFiles(files); */
}

getResume();